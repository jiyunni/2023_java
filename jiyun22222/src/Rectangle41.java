public class Rectangle41 {
    int width;
    int height;

    // 넓이를 반환하는 메소드
    int area() {
        return width * height;
    }

    // 둘레를 반환하는 메소드
    int perimeter() {
        return 2 * (width + height);
    }
}
